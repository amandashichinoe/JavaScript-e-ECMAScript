# Aprendizados

    Conversão entre tipos
        Number.parseInt(n)
        Number.parseFloat(n)
        Number(n)
        String(n)
        n.ToString()
    
    Formatando strings
        s.Length
        s.toUpperCase()
        s.toLowerCase()
    
    Formatando números
        n1.toFixed(n)
        n1.toFixed(n).replace('.',',')
        n1.toLocaleString('pt-BR', {style: 'currency', currency:'BRL'})